name: Publish to RubyGems.org

on:
  push:
    branches: main
    paths: lib/angry_io/version.rb
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: rubygems-publish
    if: github.repository_owner == 'bquorning'
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: false
          ruby-version: "4.0"
      - name: Install dependencies
        run: bundle install
      - uses: rubygems/release-gem@v1
